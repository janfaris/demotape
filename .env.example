# Pro license key (optional — required for multi-format, overlays, Supabase auth, AI features)
DEMOTAPE_LICENSE_KEY=

# OpenAI API key (required for AI features: generate, narration, visual readiness)
OPENAI_API_KEY=

# Supabase auth (optional — only needed if using the supabase auth provider)
DEMOTAPE_SUPABASE_URL=
DEMOTAPE_SUPABASE_ANON_KEY=
DEMOTAPE_SUPABASE_SERVICE_ROLE_KEY=
DEMOTAPE_EMAIL=

# General
HEADLESS=true
